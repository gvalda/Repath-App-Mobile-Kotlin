<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:layout_marginBottom="10dp"
    android:layout_marginHorizontal="20dp"
    android:background="@drawable/bg_report_item">

    <ImageView
        android:id="@+id/obstacle_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintBottom_toBottomOf="@id/obstacle_location"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/obstacle_title"
        app:srcCompat="@drawable/ic_obstacle_list_item" />

    <TextView
        android:id="@+id/obstacle_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        app:layout_constraintStart_toEndOf="@id/obstacle_icon"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/obstacle_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/obstacle_title"
        app:layout_constraintTop_toBottomOf="@id/obstacle_title" />

</androidx.constraintlayout.widget.ConstraintLayout>